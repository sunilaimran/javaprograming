package day014_String;

public class example {
    public static void main(String[] args) {

 /*
   at given index
String Methods:

charAt (index): returns the character at the given index, returns char
                  String s = "Python"
                  s.charAt(3) ==> 'h'

length():returns total number of characters, returns int

last index: length() -1 <- to find last index

toLowerCase(): returns new String in lowerCase version of old String object

toUpperCase(): returns new String in the upper case version of old string object

equals(): checks if two strings have the same text, return boolean

trim() method: removes white space

indexof(str): returns the index number of first occurred chracter, returns it as an int

lastIndexOf(str): returns you the index number of last occurred character, returns as an int

replace(oldValue, newValue): replaces of value with new value

equals(): checks if two strings have the same text, returns boolean

replaceFirst(): only replaces first matching values

substring(beginningIndex, endingIndex): creates substring starting from the given beginning index until the ending index
(endingIndex is excluded)'String str = "Cydeo School';
str.substring(0, 4+1) ==> Cydeo

substring(beginningIndex): creates substring starting from the given beginning index til the end of the String
String str = "Cydeo School";
str.substring(6) ==> School
  */


    }
}
